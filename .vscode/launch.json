{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Next.js: debug server-side (backend)",
      "type": "node-terminal",
      "request": "launch",
      "command": "pnpm dev",
      "env": {
        "NODE_OPTIONS": "--inspect"
      },
      "skipFiles": [
        "<node_internals>/**",
        "${workspaceFolder}/node_modules/**"
      ]
    },
    {
      "name": "Next.js: debug client-side (frontend)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:3000",
      "pathMapping": {
        "/": "${workspaceFolder}/",
        "/static": "${workspaceFolder}/.next/static"
      }
    },
    {
      "name": "Next.js: debug full stack",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/node_modules/next/dist/bin/next",
      "runtimeArgs": ["--inspect"],
      "skipFiles": [
        "<node_internals>/**",
        "${workspaceFolder}/node_modules/**",
        "${workspaceFolder}/.next/**"
      ],
      "serverReadyAction": {
        "action": "debugWithChrome",
        "killOnServerStop": true,
        "pattern": "- Local:.+(https?://.+)",
        "uriFormat": "%s",
        "webRoot": "${workspaceFolder}"
      }
    }
  ]
}
