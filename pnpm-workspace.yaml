packages: []

ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
overrides:
  postcss@<8.5.10: '>=8.5.10'
  # 1.1.12 is the patched 1.x line. An unbounded '>=1.1.18' here resolves to
  # brace-expansion 5.x, which minimatch@3 cannot call — that breaks ESLint.
  brace-expansion@1: '1.1.12'
